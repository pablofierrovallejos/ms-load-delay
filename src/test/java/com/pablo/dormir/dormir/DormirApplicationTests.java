package com.pablo.dormir.dormir;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class DormirApplicationTests {

    @Test
    void contextLoads() {
        // This test ensures that the Spring application context is loaded successfully.
        // It is intentionally left empty because its purpose is to fail if the application context cannot start.
    }

}
